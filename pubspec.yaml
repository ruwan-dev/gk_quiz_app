name: gk_quiz_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  percent_indicator: ^4.2.3
  cloud_firestore: ^6.1.3
  firebase_core: any
  firebase_auth: any
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # 🖼️ ඔයාගේ Logo එකේ Path එක විතරක් මෙතන තියෙනවා
  assets:
    - assets/logo.png

  # 🚀 සිංහල ෆොන්ට් එක මෙතනින් සම්පූර්ණයෙන්ම අයින් කළා